import { Injectable } from '@angular/core';

// TODO Day 4: Implement signal-based auth state (user, token, isAuthenticated computed).
// TODO Day 5: Replace fake login with HttpClient call.
@Injectable({ providedIn: 'root' })
export class AuthService {
  // login(email: string, password: string) { ... }
  // logout() { ... }
  // readonly user = ...
  // readonly token = ...
  // readonly isAuthenticated = ...
}
